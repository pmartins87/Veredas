name: Veredas 11.6 Structured Localization Apply (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after successful structured localization application; permanent checks live in veredas-localization.yml.'
