name: Veredas 11.6 Boss Name Localization Apply (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after successful curated boss-name localization; permanent checks live in veredas-localization.yml.'
