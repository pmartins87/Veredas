name: Veredas 11.6 Structured Name Localization Apply (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after successful structured name localization; permanent checks live in veredas-localization.yml.'
