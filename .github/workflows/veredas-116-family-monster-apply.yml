name: Veredas 11.6 Family Monster Name Localization Apply (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after successful family/monster localization; permanent checks live in veredas-localization.yml.'
