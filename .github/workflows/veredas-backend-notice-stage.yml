name: Veredas Backend Notice Staging 12.9 (Superseded)

on:
  workflow_dispatch:

permissions: {}

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded workflow marker
        run: echo 'Superseded by veredas-backend-notice-identity.yml; no staging action required.'
